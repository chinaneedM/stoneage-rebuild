# Client Archaeology

Future reports should be reproducible and record hashes, file trees, PE metadata, timestamps (with caution), strings, resource IDs, maps, UI, audio inventory, and cross-version diffs.
