name: Probe historical StoneAge full-map packages
on:
  workflow_dispatch:
  push:
    paths:
      - tools/stoneage_historical_map_pack_archive_probe.py
      - tests/test_stoneage_historical_map_pack_archive_probe.py
      - .github/workflows/probe-stoneage-historical-map-packs.yml

permissions:
  contents: write

concurrency:
  group: stoneage-historical-map-packs
  cancel-in-progress: true

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - name: Validate historical map-pack probe
        run: python3 -m unittest tests.test_stoneage_historical_map_pack_archive_probe -v
      - name: Probe historical map-package archive indexes
        run: |
          set -euo pipefail
          mkdir -p research/recovered
          python3 -m tools.stoneage_historical_map_pack_archive_probe \
            > research/recovered/STONEAGE-HISTORICAL-MAP-PACK-ARCHIVE-R1.txt
      - name: Commit derived metadata only
        run: |
          set -euo pipefail
          git config user.name stoneage-recovery-bot
          git config user.email stoneage-recovery-bot@users.noreply.github.com
          report='research/recovered/STONEAGE-HISTORICAL-MAP-PACK-ARCHIVE-R1.txt'
          git add "$report"
          git diff --cached --quiet && exit 0
          git commit -m 'research: probe historical StoneAge map-pack archives'
          if ! git pull --rebase origin main; then
            git rebase --abort || true
            git fetch origin main
            git reset --hard origin/main
            python3 -m tools.stoneage_historical_map_pack_archive_probe > "$report"
            git add "$report"
            git diff --cached --quiet && exit 0
            git commit -m 'research: probe historical StoneAge map-pack archives'
          fi
          git push
