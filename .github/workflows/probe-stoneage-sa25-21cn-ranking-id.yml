name: Recover native 21CN StoneAge 2.5 catalogue ID

on:
  workflow_dispatch:
  push:
    paths:
      - tools/stoneage_sa25_21cn_ranking_id_probe.py
      - tests/test_stoneage_sa25_21cn_ranking_id_probe.py
      - .github/workflows/probe-stoneage-sa25-21cn-ranking-id.yml

permissions:
  contents: write

concurrency:
  group: stoneage-sa25-21cn-ranking-id
  cancel-in-progress: true

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - name: Validate ranking-link parser
        run: python3 -m unittest tests.test_stoneage_sa25_21cn_ranking_id_probe -v
      - name: Recover native catalogue ID
        run: |
          set -euo pipefail
          mkdir -p research/recovered
          python3 -m tools.stoneage_sa25_21cn_ranking_id_probe \
            > research/recovered/STONEAGE-SA25-21CN-RANKING-ID-R1.txt
      - name: Commit derived metadata only
        run: |
          set -euo pipefail
          R='research/recovered/STONEAGE-SA25-21CN-RANKING-ID-R1.txt'
          cp "$R" /tmp/report.txt
          git config user.name stoneage-recovery-bot
          git config user.email stoneage-recovery-bot@users.noreply.github.com
          git fetch origin main
          git reset --hard origin/main
          mkdir -p research/recovered
          cp /tmp/report.txt "$R"
          git add "$R"
          git diff --cached --quiet && exit 0
          git commit -m 'research: recover native 21CN StoneAge 2.5 catalogue ID'
          for n in 1 2 3 4 5; do
            git push && exit 0
            sleep $((n * 2))
          done
          exit 1
