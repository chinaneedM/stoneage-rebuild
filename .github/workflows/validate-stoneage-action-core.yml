name: Validate StoneAge Action NPC core
concurrency:
  group: stoneage-action-core
  cancel-in-progress: true
on:
  push:
    paths:
      - .github/workflows/validate-stoneage-action-core.yml
      - tools/stoneage_action_core_model.py
      - tests/test_stoneage_action_core_model.py
      - research/mechanics/STONEAGE-ACTION-NPC-CORE-R1.md
permissions:
  contents: read
jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - run: python3 -m unittest tests.test_stoneage_action_core_model -v
