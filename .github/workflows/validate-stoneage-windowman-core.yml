name: Validate StoneAge Windowman core
concurrency:
  group: stoneage-windowman-core
  cancel-in-progress: true
on:
  push:
    paths:
      - .github/workflows/validate-stoneage-windowman-core.yml
      - tools/stoneage_windowman_core_model.py
      - tests/test_stoneage_windowman_core_model.py
      - research/mechanics/STONEAGE-WINDOWMAN-CORE-R1.md
permissions:
  contents: read
jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - run: python3 -m unittest tests.test_stoneage_windowman_core_model -v
