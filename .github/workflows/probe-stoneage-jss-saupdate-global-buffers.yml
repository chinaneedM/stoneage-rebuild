name: Probe archived JSS SaUpdate global buffers
on:
  workflow_dispatch:
  push:
    paths:
      - tools/stoneage_jss_saupdate_global_buffer_xref_probe.py
      - tests/test_stoneage_jss_saupdate_global_buffer_xref_probe.py
      - .github/workflows/probe-stoneage-jss-saupdate-global-buffers.yml
permissions:
  contents: write
concurrency:
  group: stoneage-jss-saupdate-global-buffers
  cancel-in-progress: true
jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
      - name: Install analysis dependencies
        run: pip install --quiet pefile capstone
      - name: Validate global-buffer probe
        run: python3 -m unittest tests.test_stoneage_jss_saupdate_global_buffer_xref_probe -v
      - name: Derive exact global-buffer xrefs
        run: |
          set -euo pipefail
          mkdir -p research/recovered
          python3 -m tools.stoneage_jss_saupdate_global_buffer_xref_probe \
            > research/recovered/STONEAGE-JSS-SAUPDATE-GLOBAL-BUFFERS-R1.txt
      - name: Verify no executable payload was retained
        run: |
          set -euo pipefail
          ! find research tools tests -type f -iname '*.exe' -print -quit | grep -q .
      - name: Commit derived metadata only
        run: |
          set -euo pipefail
          git config user.name stoneage-recovery-bot
          git config user.email stoneage-recovery-bot@users.noreply.github.com
          git add research/recovered/STONEAGE-JSS-SAUPDATE-GLOBAL-BUFFERS-R1.txt
          git diff --cached --quiet && exit 0
          git commit -m 'research: trace JSS SaUpdate global buffers'
          git pull --rebase origin main
          git push
