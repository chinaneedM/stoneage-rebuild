name: Validate StoneAge NPCEnemy core
concurrency:
  group: stoneage-npcenemy-core
  cancel-in-progress: true
on:
  push:
    paths:
      - .github/workflows/validate-stoneage-npcenemy-core.yml
      - tools/stoneage_npcenemy_core_model.py
      - tests/test_stoneage_npcenemy_core_model.py
      - research/mechanics/STONEAGE-NPCENEMY-CORE-R1.md
permissions:
  contents: read
jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - run: python3 -m unittest tests.test_stoneage_npcenemy_core_model -v
