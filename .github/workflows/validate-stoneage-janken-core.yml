name: Validate StoneAge Janken core
concurrency:
  group: stoneage-janken-core
  cancel-in-progress: true
on:
  push:
    paths:
      - .github/workflows/validate-stoneage-janken-core.yml
      - tools/stoneage_janken_core_model.py
      - tests/test_stoneage_janken_core_model.py
      - research/mechanics/STONEAGE-JANKEN-CORE-R1.md
permissions:
  contents: read
jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - run: python3 -m unittest tests.test_stoneage_janken_core_model -v
