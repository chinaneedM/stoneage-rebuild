name: Probe Sina StoneAge historical download targets
on:
  workflow_dispatch:
  push:
    paths:
      - tools/stoneage_sina_download_target_probe.py
      - tests/test_stoneage_sina_download_target_probe.py
      - .github/workflows/probe-stoneage-sina-download-targets.yml

permissions:
  contents: write

concurrency:
  group: stoneage-sina-download-targets
  cancel-in-progress: true

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - name: Validate Sina target probe
        run: python3 -m unittest tests.test_stoneage_sina_download_target_probe -v
      - name: Recover historical target links
        run: |
          set -euo pipefail
          mkdir -p research/recovered
          python3 -m tools.stoneage_sina_download_target_probe \
            > research/recovered/STONEAGE-SINA-DOWNLOAD-TARGETS-R1.txt
      - name: Commit derived metadata only
        run: |
          set -euo pipefail
          git config user.name stoneage-recovery-bot
          git config user.email stoneage-recovery-bot@users.noreply.github.com
          report='research/recovered/STONEAGE-SINA-DOWNLOAD-TARGETS-R1.txt'
          git add "$report"
          git diff --cached --quiet && exit 0
          git commit -m 'research: recover Sina StoneAge download targets'
          if ! git pull --rebase origin main; then
            git rebase --abort || true
            git fetch origin main
            git reset --hard origin/main
            python3 -m tools.stoneage_sina_download_target_probe > "$report"
            git add "$report"
            git diff --cached --quiet && exit 0
            git commit -m 'research: recover Sina StoneAge download targets'
          fi
          git push
